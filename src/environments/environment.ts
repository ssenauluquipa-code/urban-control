export const environment = {
  production: false,
  apiUrl: '/api/v1' // Desarrollo (El proxy de Angular redirige esto a la URL local o de pruebas)
  // apiUrl: 'https://urbancontrol-api-io2l.onrender.com/api/v1' // Producción (Render)
};
